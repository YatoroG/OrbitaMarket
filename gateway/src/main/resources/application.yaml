server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: payments-service
          uri: http://payments-service:8082
          predicates:
            - Path=/api/accounts/**, /v3/api-docs/accounts
